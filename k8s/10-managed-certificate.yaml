apiVersion: networking.gke.io/v1
kind: ManagedCertificate
metadata:
  name: employee-platform-cert
  namespace: employee-platform
spec:
  domains:
    - app.employee-platform.example
    - api.employee-platform.example
